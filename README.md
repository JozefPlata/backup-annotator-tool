### Requirements:

`Python 3.12+`

### Installation:

It should be quite straightforward, all you need is a `.venv` with dependencies from `requirements.txt`.

### Running:

    `flask --app main run`
